import React from 'react';
import Debug from 'debug';

const debug = Debug('DocCard');
debug.enabled = true;

/**
 * Represents a card that displays the contents and allows editing of a
 * document in the database.
 *
 * The objective is to dynamically populate the doc card with the properties of
 * the document it is being provided.
 *
 * First step is to get the data into the application though likely. Or
 * atleast allow it to be stored somewhere.
 *
 * @param {DocCardProps} props the props for the DocCard
 */
function DocCard() {
  return <div>tes</div>;
}

export default DocCard;
