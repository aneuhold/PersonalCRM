/**
 * The purpose of this file is to instantiate the data for all documents for
 * the user.
 */

// Create a singleton class for each type of data that the db has.
const clientData = {};

export default clientData;
